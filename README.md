ericnishio.github.io
====================

## Setup

Requirements: *Ruby*

```
$ gem install bundler jekyll
$ bundle install
```

## Start dev server

```
$ bundle exec jekyll serve
```

Include drafts:

```
$ bundle exec jekyll serve --drafts
```
